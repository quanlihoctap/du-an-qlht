import 'package:flutter/material.dart';

class StudyTab extends StatelessWidget {
  const StudyTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Học tập', style: TextStyle(fontSize: 24)),
    );
  }
}
